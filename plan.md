*Pendiente crítico*

1. sendAgent() funcional con fetch al backend
2. Arreglo de mensajes en JS (messages[] con objetos {role,content})
3. Render de mensajes en DOM con createElement — no innerHTML
4. Validación del input antes del fetch
5. Modal de configuración del agente
6. USO_IA.md

El orden natural es: 2 → 3 → 1 → 4 → 5 → 6.